language: objective-c
osx_image: xcode10.2

branches:
  except:
    - localization

env:
  global:
    - THEOS=theos

script:
- make package

before_install:
- brew install ldid
- git clone --recursive https://github.com/Shade-Zepheri/theos $THEOS